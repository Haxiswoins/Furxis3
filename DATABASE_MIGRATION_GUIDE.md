# 数据存储迁移指南：从本地JSON到云数据库

**版本**: 2.0
**目标**: 将网站的数据存储从项目内的JSON文件，迁移到一个由专业云服务商（如腾讯云、阿里云）提供的、独立的、高安全性的托管数据库系统。

---

## 概述

您当前的网站将所有数据以明文JSON文件的形式存储在 `src/data/` 目录中，这存在严重的安全和性能瓶颈。本指南将指导您完成一个标准的、生产级的迁移流程。

**核心架构**：我们将采用经典的**前后端分离**架构。
1.  **前端 (Next.js)**：保持不变，继续作为用户界面。
2.  **后端API服务 (新建)**：您需要在您的云服务器上创建一个新的、独立的Node.js服务。这个服务是**唯一**有权访问云数据库的程序。
3.  **云数据库 (新建)**：您将在您的云服务商（以腾讯云为例）处购买一个托管的数据库实例（以PostgreSQL为例）。

**数据流**：用户在网站上的操作 -> Next.js前端调用后端API -> 后端API服务验证请求并连接云数据库进行操作。

---

## 第一步：在云服务商（如腾讯云）创建数据库实例

1.  **登录腾讯云控制台**：
    进入“云产品” -> “数据库” -> “云数据库 PostgreSQL”。

2.  **新建实例**：
    点击“新建”，根据您的需求选择地域、可用区、数据库版本（推荐12或更高版本）和计算规格。对于初期项目，选择最低规格即可。**请务必设置一个高强度的管理员密码**，并妥善保管。

3.  **获取连接地址（最关键）**：
    实例创建成功后，进入实例详情页。您需要找到“**外网地址**”或“**公网地址**”，这通常是一个形如 `your-instance-id.ap-guangzhou.postgresql.tencentcdb.com` 的域名。记下这个地址和端口号（通常是`5432`）。

4.  **设置安全组（防火墙）**：
    这是**极其重要**的一步。为了安全，云数据库默认禁止所有外网访问。您必须配置安全组，**只允许**您的云服务器来访问它。
    *   在实例详情页找到“安全组”配置。
    *   添加入站规则：
        *   **来源**: 输入您**云服务器的公网IP地址**。
        *   **协议端口**: `TCP:5432`
        *   **策略**: 允许
    *   **如果您不确定如何操作，请联系腾讯云客服获取帮助。错误的配置将导致您的API服务无法连接到数据库。**

5.  **创建专用数据库和用户**：
    使用任何数据库客户端工具（如Navicat, DBeaver, 或psql）通过**外网地址**连接到您的云数据库实例（使用您创建时设置的管理员账户和密码）。连接成功后，执行以下SQL命令：

    ```sql
    -- 1. 创建一个新的数据库
    CREATE DATABASE forward_infinity_db;

    -- 2. 创建一个新的、用于API服务的专用用户，并设置一个极其强壮的密码
    -- ！！！极其重要：您的密码必须用单引号 ' ' 包裹起来！
    CREATE USER forward_infinity_api_user WITH PASSWORD '一个非常非常复杂的随机密码';

    -- 3. 将新数据库的所有权限授予这个新用户
    GRANT ALL PRIVILEGES ON DATABASE forward_infinity_db TO forward_infinity_api_user;
    ```

6.  **拼接并记录您的数据库连接字符串 (DATABASE_URL)**：
    您的连接字符串格式如下。请将其拼接好并妥善保管，下一步会立即用到。**请将占位符替换为真实值**。

    `postgresql://forward_infinity_api_user:一个非常非常复杂的随机密码@your-instance-id.ap-guangzhou.postgresql.tencentcdb.com:5432/forward_infinity_db?ssl=true`

    > **⚠️ 注意**：大多数云数据库强制要求使用SSL加密连接。所以在连接字符串的末尾加上 `?ssl=true` 是一个非常好的安全实践。

---

## 第二步：创建独立的后端API服务

这个新的服务将和您的Next.js应用一起，在您的云服务器上用PM2运行。

1.  **在服务器上创建项目文件夹**:
    在您喜欢的位置（例如 `/var/www/`）创建一个新文件夹：
    ```bash
    # 登录您的云服务器
    ssh your_user@your_server_ip

    mkdir /var/www/forward_infinity_api
    cd /var/w