# 网站图片尺寸与比例指南

本文档旨在为您提供网站各个模块所需图片的最佳尺寸和宽高比建议，以确保在所有设备上都能获得最佳的视觉效果和加载性能。

---

### **核心原则**

*   **比例最重要**：对于大多数图片框，只要您上传的图片**比例正确**，它就能很好地显示。我们提供的建议尺寸是在此基础上，确保在高分辨率屏幕上依然清晰。
*   **稍大优于稍小**：提供比建议尺寸稍大的图片是安全的，Next.js 会自动进行优化。
*   **上传前压缩**：强烈建议在上传前使用 [TinyPNG](https://tinypng.com/) 或 [Squoosh](https://squoosh.app/) 等工具压缩图片。

---

## **一、 全局与首页**

### **1. 首页背景图 (`/home`)**
   - **用途**：作为主页的全屏背景。
   - **文件**：在`后台 > 页面内容管理 > 全局设置`中配置。
   - **建议宽高比**：`16:9` (标准宽屏)
   - **建议尺寸**：**1920 x 1080 像素**

### **2. 首页三大导航卡片 (`/home`)**
   - **用途**：委托申请、设定领养、作品一览的封面图。
   - **文件**：在`后台 > 页面内容管理`中对应区域配置。
   - **建议宽高比**：`4:5` (竖向)
   - **建议尺寸**：**800 x 1000 像素**

---

## **二、 设定领养 (`/adoption`)**

### **1. 领养系列封面 (`/adoption`)**
   - **用途**：展示不同设定系列的横向全景图。
   - **文件**：在`后台 > 设定系列管理`中配置。
   - **建议宽高比**：`16:7` (宽幅全景)
   - **建议尺寸**：**1920 x 840 像素**

### **2. 领养角色图片 (`/adoption/.../角色详情页`)**
   - **用途**：角色详情页右侧的图片展示区。
   - **文件**：在`后台 > 领养角色管理`中配置。
   - **建议宽高比**：`3:4` (竖向)
   - **建议尺寸**：**900 x 1200 像素**
   - **备注**：一个角色最多支持5张图片（1张主图，4张详情图），建议全部使用此比例。

---

## **三、 委托申请 (`/commission`)**

### **1. 各期委托封面 (`/commission`)**
   - **用途**：展示不同委托期的横向封面图。
   - **文件**：在`后台 > 各期委托管理`中配置。
   - **建议宽高比**：`16:9` (标准宽屏)
   - **建议尺寸**：**1280 x 720 像素**

### **2. 委托样式图片 (`/commission/...`)**
   - **用途**：在委托样式表单中，作为样式的示例图。
   - **文件**：在`后台 > 委托样式管理`中配置。
   - **建议宽高比**：`1:1` (方形) 或 `4:5` (竖向)
   - **建议尺寸**：**800 x 800 像素** 或 **800 x 1000 像素**
   - **备注**：由于此处的卡片没有固定比例，方形或竖向图片均可良好适应。

### **3. 用户上传的设定参考图**
   - **用途**：用户在填写委托申请时上传的参考图片。
   - **建议尺寸**：无严格要求，但建议用户上传**小于 5MB** 的清晰图片。

---

## **四、 作品展示 (`/works`)**

### **1. 作品集主图与详情图**
   - **用途**：作品列表页的封面和详情页的图片。
   - **文件**：在`后台 > 作品管理`中配置。
   - **建议宽高比**：`3:4` (竖向)
   - **建议尺寸**：**900 x 1200 像素**
   - **备注**：虽然详情页是错落布局，但统一使用此竖向比例能获得最佳的排版效果。

---

## **五、 后台管理**

后台管理中的所有列表（如订单、角色、作品列表等）中显示的都是**缩略图**，它们是根据您在上述模块中上传的原图自动生成的，**无需单独准备**。
